<%# @TODO
Tidy this up. Not yet certain how to do this.
%>
<div class="<%= new.dom_class %>">
  <%= label_tag("work_#{new.predicate}", new.t(:predicate)) %>
  <div class="values">
  <% new.each_with_index do |value, index| %>
    <%= text_field_tag("work_#{new.predicate}_#{index+1}", value, name: "work[#{new.predicate}][]") %>
  <% end %>
    <%= text_field_tag("work_#{new.predicate}_0", nil, name: "work[#{new.predicate}][]") %>
  </div>
</div>
